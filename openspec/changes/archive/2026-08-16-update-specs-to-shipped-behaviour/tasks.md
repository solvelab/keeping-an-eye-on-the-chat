## 1. Correct what is wrong
- [x] 1.1 Replace the Twitch-only required-field requirement in `configure-app`.
- [x] 1.2 Correct the multi-source configuration requirement in `observe-chat` to the shipped fields.
- [x] 1.3 Remove the migration scenario describing work that was never implemented.
- [x] 1.4 Give `observe-chat` a Purpose instead of the archiving placeholder.

## 2. Cover what is missing
- [x] 2.1 Author-name presentation as a chosen treatment.
- [x] 2.2 The wizard's bubble preview, including that it uses the overlay's own stylesheet.
- [x] 2.3 Stopping and resuming observation from the tray without ending the app.

## 3. Validation
- [x] 3.1 `openspec validate update-specs-to-shipped-behaviour --strict`.
- [x] 3.2 Every claim traced to a line of shipped code before it is written down.
